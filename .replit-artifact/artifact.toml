kind = "web"
previewPath = "/"
title = "Football Simulation Engine"
version = "1.0.0"
id = "artifacts/football-sim"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/football-sim run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/football-sim", "run", "build" ]
publicDir = "artifacts/football-sim/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
